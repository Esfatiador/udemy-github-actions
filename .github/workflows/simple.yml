name: shell commands

on: [push]

jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "hello Word"
      - name: multiline script
        run: |
          node -v
          npm -v
      - name: python commands
        run:  |
          import platform
          print(platform.processor())
        shell: python
   run-Windows-commands:
  runs-on: Windows-latest  
  steps:
    - name: Directory Powershell
      run: Get-Locatin # In Bash wold be run "pwd"
    - name: Directory Bash
      run: pwd
      shell: bash  
